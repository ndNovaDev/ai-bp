// 周报落盘的路径/标题工具。起草本身完全由主对话驱动,本模块不持有任何 prompt。
//
// 历代风格控制大多证伪(STYLE_GUIDE / AUDITOR_LENS / AI_TELLS / Peterson 多步骤 / 多路 peer review / 摸语气画像)。
// 教训:列负面规则、列变换维度 = 均值化锚点。
//
// 当前方案在 commands/ai-practice-pick.md 步 5 + 步 6 + 步 7 + 步 8:
//   步 5 采访  :三小步,严格按序 ——
//                5.0 背景盘点:Claude 列"读完证据后仍不清楚的业务背景"(谁提的 / 上下游 / 痛点性质 / deadline),
//                              缺口 ≥2 条必须走一次 AskUserQuestion 问清。不走 → 默默猜 → 默默错。
//                              0.1.44 新增。之前直接跳 5.1,模型拿到证据就开始构思方向,
//                              根本没机会承认"我不懂业务背景"。
//                5.1 草简述  :输出 "方向"(2-4 句) + "重点写 / 略过"(各 2-4 条 + 略过原因)。
//                              0.1.44 显式把取舍清单单独列出 —— 之前只草方向,用户看不出哪些被砍了,
//                              结果模型默认事无巨细,周报变 changelog。
//                5.2 拍板    :AskUserQuestion 二选一 "采纳" / "我自己来",
//                              Other 里填重点 / 砍掉 / 换切入点 / 自己的简述都行。
//                              注:options 是决策不是输入形态分类 —— 0.1.37 把选项写成
//                              "大纲/简述/要点"分类,用户以为选了某项就会得到那类东西,
//                              结果选了之后什么都没发生。0.1.38 改成"采纳/我自己来"。
//   步 6 第一版:四条正向方向(王小波口吻 / 金字塔原理 / 知识诅咒 guard / 读者画像)。
//                0.1.44 把读者画像从"审稿 AI + 领导"收窄成"领导(技术背景,但不读代码)"
//                + 加硬性禁止清单(commit hash / 版本号 / 文件路径 / 行号 / 函数名,除非本身是结论)。
//                之前"审稿 AI"反向诱导模型塞 artifact 给 AI 审计真伪,周报塞满 commit 号。
//   步 7 段落级重排 + 加小标题:
//                7.1-7.3 (0.1.44):结论段强制最前 + 砍冗余段 + 检查禁止清单。
//                7.4 (0.1.47):每段必须配 H3 小标题。之前没硬性要求,模型默认整篇连续 paragraph,
//                              领导根本读不下去 —— 用户原话"文档还是要分段落标题的,不然人没法看"。
//                先动段落骨架,后面步 8 才动句子。子句打散救不了"背景→实现→收尾"这种 LLM 默认形状。
//   步 8 句式打散:逐子句重写,长短随机 + 句式随机 + 王小波口吻,唯一硬约束是通顺。
//                不列变换维度,不预设 SOP。(原步 7,因新增段落级重排往后挪一位。)
//                0.1.47 把"必做"升级成"必经,严禁跳过",加 ⚠️ 顶栏 —— 模型有时觉得第一版"够好"
//                就直接跳到落盘,导致整篇前功尽弃。
//   隐性硬约束 :只锚 user 原话,不锚 Haiku 摘要。
//
// 步 8 表面看像 v3.x 那版"5h 句式打散"复活,但关键差异:
//   v3.x: 列了"换起头/动词/修辞/句式"四个具体维度 + "能抠出 SOP 就推倒重做"自指悖论 → 失败
//   现在: 只一条"通顺"硬约束,变换维度让模型自己随机
//
// 详见 git log 0.1.20–0.1.47。

function titleToSlug(title) {
  if (!title) return 'untitled';
  let s = String(title).trim().toLowerCase();
  s = s.replace(/[\s　]+/g, '-');
  s = s.replace(/[^\p{L}\p{N}\-]/gu, '');
  s = s.replace(/-+/g, '-').replace(/^-|-$/g, '');
  if (!s) return 'untitled';
  return s.length > 60 ? s.slice(0, 60).replace(/-+$/, '') : s;
}

function extractTitle(markdown) {
  const m = /^#\s+(.+)$/m.exec(markdown || '');
  return m ? m[1].trim() : 'untitled';
}

module.exports = {
  titleToSlug,
  extractTitle,
};
